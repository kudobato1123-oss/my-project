version: '3.8'
services:
  production-app:
    build: .
    container_name: production-app
    ports:
      - "3000:3000"
      - "3002:3002"
      - "3005:3005"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
