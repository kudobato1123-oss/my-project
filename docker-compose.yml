version: '3.8'

services:
  production-app:
    build: .
    container_name: production-app
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
